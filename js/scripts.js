
            // Получаем текущий год
            const currentYear = new Date().getFullYear();
            // Вставляем текущий год в элемент с id="year"
            document.getElementById('year_creation').textContent = currentYear;
